pipeline {
    
    agent { dockerfile true }
    
    stages {
        
        stage("build") {
            
            steps {
                
                echo 'build'
                
            }
        } 
        
        stage("deploy") {
            
            steps {
                 echo 'deploy'
            }
        }
    }
}
